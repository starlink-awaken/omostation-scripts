name: gBrain CI (no-op — CI lives in gbrain repo)
on: []  # archived - scripts repo deprecated
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "projects/gbrain is .gitignore'd — its CI lives in the gbrain repo."
