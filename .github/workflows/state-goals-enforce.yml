name: state-goals-enforce
# P47+: state/goals 对齐 enforce.
# 原引用 scripts/check-state-goals-alignment.py — 该文件在仓库中从未存在
# (tests/test_state_goals_alignment.py 也依赖它), 每次 push/PR 恒红.
# state/goals 对齐在仓库中未实现独立检查; 禁用触发器 (保留定义以便未来补实现
# 后恢复). 如需恢复, 将 on: [] 改为 on: [push, pull_request] 并指向真实脚本.
on: []

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: "3.13"}
      - run: pip install pyyaml
      - name: State/goals alignment check (not implemented)
        run: echo "state/goals alignment check not implemented; disabled pending real script"
